{
  "mcpServers": {
    "teamkb": {
      "name": "teamkb",
      "description": "Governed team knowledge brain — read-only cited search (teamkb_search). Returns qmd:// citations. Proxies to the brain over the tailnet; install-once, no build.",
      "version": "0.4.0",
      "enabled": true,
      "transport": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/plugin-runtime/teamkb-remote.cjs"],
      "env": {
        "TEAMKB_API_URL": "${TEAMKB_API_URL}",
        "TEAMKB_API_TOKEN": "${TEAMKB_API_TOKEN}"
      }
    }
  }
}
